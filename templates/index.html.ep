% layout 'default';
% title 'ITIL Desk';
% if ( my $message = flash('error') ) {
<div style="color: red"><%= $message %></div>
% }
<h1>Welcome to ITIL Desk</h1>

<table>
  <tr>
    <td><%= include 'desk', deskname=>'incident', desk=>$game->{desks}{incident} %></td>
    <td><%= include 'desk', deskname=>'problem', desk=>$game->{desks}{problem} %></td>
  </tr>
  <tr>
  </tr>
</table>

<pre>
%= dumper $game
</pre>
Desk name:
<%# = (keys %{$game->{desks}})[0] %>


