% layout 'default';
% title 'ITIL Desk';
% if ( my $message = flash('error') ) {
<div style="color: red"><%= $message %></div>
% }

<h1>Welcome to ITIL Desk</h1>

<%= include 'desk', deskname=>'incident', desk=>$game->{desk}{incident} %>
<%= include 'desk', deskname=>'problem', desk=>$game->{desk}{problem} %>

<pre>
%= dumper $game
</pre>
Desk name:
<%# = (keys %{$game->{desk}})[0] %>


