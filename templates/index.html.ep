% layout 'default';
% title 'ITIL Desk';
<h1>Welcome to ITIL Desk</h1>


<% foreach my $i (sort keys %($game->{desks}})) { %>
<table>
<% foreach my $c (qw(tickets person)) { %>
  <tr>
    %= $c
  </tr>
  <% } %>
</table>
<% } %>




Desk Name: %= (keys %{$game->{desks}})[1]

<pre>
%= dumper $game
</pre>
Desk name:
<%# = (keys %{$game->{desks}})[0] %>


